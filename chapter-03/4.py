#Replace the double space from problem 3 with single spaces.

name = "this text contains doube  space  so find it"

replaced_text = name.replace("  "," ")

print(name,"after replacing double space with single space is",replaced_text)